{
  "name": "foodog-tiffin-store",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check app.js && node --check admin.js && node --check hero-3d.js && node --check server/catalog.js"
  },
  "engines": {
    "node": ">=20"
  }
}
